package Arrays;

public class Main {
    /*Java uses stable algorithom based on mergesort adaptation of timsort
    for non primitive data types
    and dual pivot quick sort for primitive data types
    Arrays.sort(array_name,start_idx,end_idx);
    1. class class_name implements Comparable<class_name>
    2. class class_name implements Comparator<class_name>
       it takes two object
       public int compare(){}
       Arrays.sort(arr,new class_name());
       for comparator we have to create two class
     Collection implements comparable function
     */
}
